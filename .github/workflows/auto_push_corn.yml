name: auto corn
on:
  schedule:
    - cron: '*/1 * * * *' # every 1 minute
  workflow_dispatch:

jobs:
  auto_workflow_test:
    runs-on: ubuntu-latest

    steps:
      - name: first step
        run: |
          echo "Hello, World"
