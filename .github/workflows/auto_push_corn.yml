name: auto corn
on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  auto_workflow_test:
    runs-on: ubuntu-latest

    steps:
      - name: first step
        run: |
          echo "Hello, World"