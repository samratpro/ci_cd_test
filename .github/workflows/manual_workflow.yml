name : first workflow
on : 
  workflow_dispatch

jobs :
  test_manual_workflow :
    runs-on : ubuntu-latest

    steps :
      - name : first step
        run : |
          echo "Hello, World!"
    