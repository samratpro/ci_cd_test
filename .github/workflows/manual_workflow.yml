name : manual workflow
on : 
  workflow_dispatch:

jobs :
  manual_workflow_test :
    runs-on : ubuntu-latest

    steps :
      - name : first step
        run : |
          echo "Hello, World!"
    